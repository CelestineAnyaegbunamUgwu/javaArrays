//Empty Array
let myarr=[];

//Array with more than 5 element
let array5 = new Array(6);
//get lenght
let arr = [1,2,2,4];
arr.length;
//Get the first item, the middle item and the last item of the array
arr[0];//gets the first
arr[arr.length];// gets the last element